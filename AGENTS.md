# Agentic RAG 仓库级 AI 开发约束

本文件对整个仓库生效。AI 在分析、修改或生成代码前，必须先读取本文件；进入子目录后，还必须读取离目标文件最近的 `AGENTS.md`。子目录规则可以收紧本规则，不得放宽安全、质量与验证要求。

## 1. 规则优先级与项目边界

1. 用户当前明确指令优先。
2. 目标文件最近的 `AGENTS.md` 优先于上级文件。
3. `docs/开发规范与技术选型.md` 是产品与总体架构基线。
4. `apps/web/docs/frontend-engineering-standards.md` 是前端详细规范。
5. `apps/api/docs/backend-engineering-standards.md` 是 Go 后端详细规范。

正式开发区域：

- `apps/web`：React 19 + TypeScript 前端。
- `apps/api`：Go + Gin 业务后端，唯一公开业务入口。
- `backend`：现有 Python Agent，仅作为内部 RAG 运行时复用；除非任务明确要求，不得重构、迁移或扩大其职责。
- `archive`：历史归档，不得作为新代码模板，不得修改。

## 2. 不可违反的工程原则

- MUST 保持高内聚、低耦合；业务能力按模块归属，跨模块只通过稳定的公开接口协作。
- MUST 让依赖指向稳定的业务规则；领域/应用层不得依赖 HTTP、ORM、缓存、队列或供应商 SDK。
- MUST 优先设计“小接口、深实现”的模块。只有一个实现且没有测试替身需求时，不得为“可能以后替换”提前造接口。
- MUST 在变更前搜索现有实现、调用点、测试、契约与文档；不得凭文件名猜测行为。
- MUST 做最小且完整的变更。禁止顺手重构无关代码、批量改名或格式化整个仓库。
- MUST 将外部输入视为不可信：HTTP、文件、数据库、缓存、消息队列、模型输出和环境变量都要在边界验证。
- MUST 对失败建模。禁止吞错、空 catch、忽略关键返回值、用假成功掩盖未实现能力。
- MUST 保持单一事实源；不得把同一服务端数据同时复制到多个客户端状态容器，也不得让 Go 与 Python 写同一业务表。
- MUST 保持向后兼容，或在契约、迁移、客户端和发布说明中显式处理破坏性变化。
- MUST 在提交结果前运行与变更匹配的格式化、静态检查、测试和构建，并报告真实结果。

## 3. AI 强制工作流

### 修改前

1. 读取适用的全部 `AGENTS.md` 和对应详细规范。
2. 确认任务目标、受影响模块、公开契约、数据所有者和信任边界。
3. 检查工作区现有变更，不覆盖用户工作。
4. 对跨模块、数据库、鉴权、异步副作用或公开接口变更，先说明依赖方向和失败/回滚路径。

### 实现中

- 不新增未经批准的框架、状态库、ORM、消息系统或基础设施。
- 不绕过既有模块公开入口进行深层导入。
- 不硬编码密钥、环境地址、租户 ID、权限或生产配置。
- 不把 TODO、mock、假数据或固定成功响应描述成已完成能力。
- 新增依赖必须说明必要性，锁定版本，并检查维护状态、许可证和已知风险。
- 代码行为、OpenAPI、迁移、测试和文档必须同步更新。

### 完成前

- 前端至少执行：`npm run typecheck`、`npm test`、`npm run build`；用户旅程变化还要执行相关 Playwright 用例。
- Go 后端至少执行：`gofmt`、`go vet ./...`、`go test ./...`；契约变化还要校验 OpenAPI，数据变化还要验证迁移。
- 不得声称未运行的检查已通过。若环境阻塞，报告命令、阻塞原因与剩余风险。

## 4. 需要显式决策的变更

以下变更必须新增或更新 ADR/设计文档后才能实现：技术栈替换、模块边界调整、跨模块数据库访问、数据所有权变化、认证授权模型变化、公开接口破坏性变化、新基础设施、新后台任务语义、加密或密钥策略变化。

ADR 至少记录：背景、决策、备选方案、依赖方向、数据所有权、安全影响、迁移/回滚方案和验证证据。

## 5. 完成定义

只有同时满足以下条件才算完成：行为满足需求；模块边界未被破坏；失败路径明确；安全与权限测试覆盖；契约和文档同步；相关检查通过；没有新增秘密、调试后门、静默降级、无主 goroutine/订阅或无法恢复的迁移。
